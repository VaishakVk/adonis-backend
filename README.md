# adonis-backend

## Steps to run 
- Make sure Adonis is installed on the system.
- Prepare `.env` file.
- Run `npm install` to download all dependencies
- Run `node ace migration:run` to create the Database and tables
- Run `npm run dev` to start.
